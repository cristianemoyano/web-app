build:
  docker:
    web: Dockerfile

release:
  image: web
  command:
    - ./deploy-tasks.sh

run:
  web: pipenv run gunicorn --env DJANGO_SETTINGS_MODULE=core.settings.prod core.wsgi:application --bind 0.0.0.0:$PORT
