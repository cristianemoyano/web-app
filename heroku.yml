build:
  docker:
    web: production.Dockerfile

release:
  image: web
  command:
    - ./deployment_tasks.sh

run:
  web: cd backend && gunicorn --env DJANGO_SETTINGS_MODULE=core.settings.prod core.wsgi:application --bind 0.0.0.0:$PORT
